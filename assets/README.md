# Assets

Application icons, screenshots, and other static branding files will live here.

v0.1.x ships without custom artwork. The GUI uses the Qt widget set and does
not load files from this directory yet.
