# Runtime wheels bundled into the Ubuntu 24.04 package-owned venv.
# Ubuntu noble does not ship python3-pyside6. Essentials covers QtWidgets/QtGui/QtCore.
PySide6_Essentials==6.11.2
# Secret Service stack. scripts/build-deb.sh installs this project with --no-deps,
# so pyproject.toml's keyring>=25 would otherwise be omitted from a clean .deb.
# Pins match the live-tested Ubuntu 24.04 python-keyring SecretService backend.
keyring==25.7.0
SecretStorage==3.5.0
jeepney==0.9.0
jaraco.classes==3.4.0
jaraco.context==6.1.2
jaraco.functools==4.6.0
more-itertools==11.1.0
cryptography==50.0.1
cffi==2.1.1
pycparser==3.0
